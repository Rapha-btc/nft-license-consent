[project]
name = "nft-license-consent"
description = "On-chain consent registry for NFT collection license changes"
authors = []
telemetry = false
cache_dir = "./.cache"

[contracts.license-consent]
path = "contracts/license-consent.clar"
clarity_version = 5
epoch = 'latest'

# test-only mock of the Gamma collection template (bitcoin-pepe shape)
[contracts.mock-gamma-collection]
path = "contracts/mocks/mock-gamma-collection.clar"
clarity_version = 5
epoch = 'latest'

# test-only stand-in for a smart-wallet / safe artist that forwards a sign call
[contracts.mock-artist-safe]
path = "contracts/mocks/mock-artist-safe.clar"
clarity_version = 5
epoch = 'latest'
